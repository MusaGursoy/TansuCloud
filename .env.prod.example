# Sample production environment for Docker Compose
# Copy to .env and adjust values for your environment

# Public HTTPS base URL of your gateway (no trailing slash)
PUBLIC_BASE_URL=https://apps.example.com

# Dashboard OIDC confidential client secret
DASHBOARD_CLIENT_SECRET=changeit

# Postgres and PgCat credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeit
PGCAT_ADMIN_USER=admin
PGCAT_ADMIN_PASSWORD=changeit

# Storage service presign secret (used to validate presigned URLs)
STORAGE_PRESIGN_SECRET=changeit

# Optional: Observability profile (SigNoz)
# Set OTLP endpoint so app services export telemetry to the in-cluster collector
OTLP_ENDPOINT=http://signoz-otel-collector:4317
# JWT secret used by SigNoz UI; set only if you enable the observability profile
SIGNOZ_JWT_SECRET=changeit

# Optional: If enabling HTTPS on the gateway with a PFX mounted under ./certs
# Kestrel will read the password from this env var (do not commit real secrets)
GATEWAY_CERT_PASSWORD=changeit
