# Unified environment configuration for TansuCloud services
# Update these values per environment; this single file is the source of truth for compose deployments.

## Core URLs
# Browser-visible base URL (include scheme, no trailing slash) https://mybackend.mydomain.com
PUBLIC_BASE_URL=http://127.0.0.1:8080
# Internal gateway URL used by containers for backchannel discovery (no trailing slash)
GATEWAY_BASE_URL=http://gateway:8080

## Database and PgCat credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
PGCAT_ADMIN_USER=admin
PGCAT_ADMIN_PASSWORD=admin

## Application secrets
# Confidential client secret for the dashboard OIDC application
DASHBOARD_CLIENT_SECRET=dev-secret
# Storage service presign secret; replace with a strong value before production
STORAGE_PRESIGN_SECRET=change-me
# Gateway HTTPS certificate password (see Guide-For-Admins-and-Tenants §5.3)
GATEWAY_CERT_PASSWORD=changeit

## Optional observability (SigNoz)
# SigNoz UI JWT secret (set only if exposing the UI)
SIGNOZ_JWT_SECRET=changemewithrandom12characters

## Product telemetry (Dashboard outbound reporter)
# HTTPS endpoint that receives aggregated product diagnostics from the Dashboard reporter.
# Leave empty to disable outbound telemetry; override per environment as needed.
LOGREPORTING__MAINSERVERURL=
# Optional bearer token for authenticating with the telemetry endpoint.
LOGREPORTING__APIKEY=dev-telemetry-api-key-1234567890
# Reporting cadence in minutes (default 60). Lower intervals increase network usage.
LOGREPORTING__REPORTINTERVALMINUTES=60
# Enable/disable the reporter (default true). Set to false to opt-out globally.
LOGREPORTING__ENABLED=true
# Optional secret used to pseudonymize tenant identifiers (HMAC SHA256). Leave empty to derive from tenant id only.
LOGREPORTING__TENANTHASHSECRET=
# Toggle pseudonymization; keep enabled unless running in isolated dev.
LOGREPORTING__PSEUDONYMIZETENANTS=true

## Telemetry ingestion service (dev defaults)
TELEMETRY__INGESTION__APIKEY=dev-telemetry-api-key-1234567890
TELEMETRY__ADMIN__APIKEY=dev-telemetry-admin-key-9876543210

## With PostgreSQL db and extension images update, automated update (executed by TansuCloud.Database service by using ALTER EXTENSION UPDATE) of extension version at tables metadata 
SKIP_EXTENSION_UPDATE=false